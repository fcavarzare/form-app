apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: form-app
  namespace: form-app-prod
  annotations:
    cert-manager.io/cluster-issuer: "selfsigned-issuer"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - form-app-prod.local
    secretName: form-app-prod-tls
  rules:
  - host: form-app-prod.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: form-app
            port:
              number: 80