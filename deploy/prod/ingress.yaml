apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: form-app
  namespace: form-app-prod
spec:
  ingressClassName: nginx
  rules:
  - host: form-app-prod.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: form-app
            port:
              number: 80