apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
  namespace: form-app-prod
spec:
  hard:
    requests.cpu: "1000m"
    requests.memory: "1024Mi"
    limits.cpu: "2000m"
    limits.memory: "2048Mi"
    pods: "10"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: form-app-prod
spec:
  limits:
  - default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    type: Container
